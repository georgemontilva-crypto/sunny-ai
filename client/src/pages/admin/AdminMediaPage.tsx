import AdminPlaceholderPage from "@/components/admin/AdminPlaceholderPage";

export default function AdminMediaPage() {
  return (
    <AdminPlaceholderPage
      title="Media"
      description="This is where you'll replace the site's images by slot, with direct upload to R2. Coming in a later phase."
    />
  );
}
