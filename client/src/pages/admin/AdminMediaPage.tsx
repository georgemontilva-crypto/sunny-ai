import AdminPlaceholderPage from "@/components/admin/AdminPlaceholderPage";

export default function AdminMediaPage() {
  return (
    <AdminPlaceholderPage
      title="Medios"
      description="Acá vas a poder reemplazar las imágenes del sitio por slot, con subida directa a R2. Llega en una fase siguiente."
    />
  );
}
