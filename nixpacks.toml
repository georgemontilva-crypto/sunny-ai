[phases.setup]
nixPkgs = ["nodejs_22", "caddy"]

[phases.build]
cmds = ["corepack enable", "pnpm install --frozen-lockfile", "pnpm build"]

[start]
cmd = "caddy run --config Caddyfile --adapter caddyfile"
